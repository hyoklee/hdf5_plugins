# This builds the PV example directory

# Build example program and run it as a test
check_PROGRAMS = async_new_h5api_ex
TESTS = run_tests.sh

# Clean up HDF5 file created by example.
CLEANFILES = *.h5

EXTRA_DIST = run_tests.sh
